version: '3.9'

services:
  bot: 
    container_name: curio_bot_${ENV}
    build:
      context: .
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - ${API_PORT}:${API_PORT}
    env_file: 
      - ${ENV_FILE}